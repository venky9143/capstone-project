apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-chat.fullname" . }}
  labels:
    {{- include "capstone-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-chat.selectorLabels" . | nindent 4 }}
